# MUST use the central region / zone for beta IoT product.
export GCLOUD_REGION=us-central1
export GCLOUD_ZONE=us-central1-c

# Kill this, since for IoT we won't deploy it.
export GOOGLE_APPLICATION_CREDENTIALS=
